grumphp:
    tasks:
        phpcs:
            standard:
                - 'phpcs.xml'
            ignore_patterns:
                - db/
        phpstan:
            configuration: 'phpstan.neon'
    fixer:
        enabled: false
        fix_by_default: false