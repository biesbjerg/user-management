{% for message in flash.get('auth') %}
    <div class="alert alert-default">
        {{ message }}
    </div>
{% endfor %}

{% for message in flash.get('default') %}
    <div class="alert alert-default">
        {{ message }}
    </div>
{% endfor %}

{{ users|json_encode }}
